{
  "name": "todo-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm -w @todo-app/api run dev\" \"npm -w @todo-app/web run dev\"",
    "build": "npm -w @todo-app/api run build && npm -w @todo-app/web run build",
    "typecheck": "npm -w @todo-app/api run typecheck && npm -w @todo-app/web run typecheck",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "concurrently": "^9.0.0",
    "eslint": "^9.9.0",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
